#include <iostream>
#include "fns.hpp"

//header files are for function declrations so the main method doesn't get bloaded
int main()
{

    std::cout << is_palindrome("noon") << "\n";
    std::cout << tenth_power(4) << "\n";
    std::cout << average(4.0, 7.0) << "\n";
}